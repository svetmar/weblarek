# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

## Данные

### Интерфейс IProduct
Информация о товаре.

Поля интерфейса:
`id: string` - идентификатор товара.
`title: string` - название товара.
`image: string` - изображение товара.
`category: string` - категория, к которой относится данный товар.
`price: number|null` - стоимость товара во внутренней валюте (синапсах). При отсутствии цены товар не доступен.
`description: string` - описание товара.

### Интерфейс IBuyer
Информация о покупателе.

Поля интерфейса:
`payment: 'card'|'cash'` - вид оплаты, возможны только 2 значения - картой онлайн или наличными.
`address: string` - адрес для доставки товара покупателю.
`email: string` - электронная почта покупателя.
`phone: string` - номер телефона покупателя.


## Модели данных

### Класс ProductCatalog
Класс хранит товары, которые можно купить в приложении.

Поля класса: 
`products: TProduct[]` - массив всех товаров.
`selectedProduct: TProduct` - товар, выбранный для подробного отображения.

Методы класса:
`setProducts(products: TProduct[]): void` - принимает в параметрах метода и сохраняет массив товаров.
`getProducts(): TProduct[]` - получение массива всех товаров из модели.
`getProductById(id: string): TProduct` - получение одного товара по его id, переданному как параметр.
`setSelectedProduct(product: TProduct): void` - принимает в параметрах метода и сохраняет выбранный покупателем товар для подробного отображения.
`getSelectedProduct(): TProduct|null` - получение выбранного товара для подробного отображения.

### Класс Cart
Класс хранит товары, которые пользователь выбрал для покупки.

Поля класса:
`items: TProduct[]` - массив товаров, выбранных покупателем для покупки.

Методы класса:
`getItems(): TProduct[]`- получение массива товаров, которые находятся в корзине.
`addItem(item: TProduct): void` - добавление товара, который был получен в параметре, в массив корзины.
`removeItem(item: TProduct): void` - удаление товара, полученного в параметре из массива корзины.
`clear(): void`- очистка корзины.
`getTotalPrice(): number` - получение стоимости всех товаров в корзине.
`getTotalQuantity(): number` - получение количества товаров в корзине.
`hasItem(id: string)` - проверка наличия товара в корзине по его id, полученного в параметр метода.

### Класс Customer
Класс содержит данные покупателя, которые тот указал при оформлении заказа.

Поля класса:
`customerData: IBuyer` - данные покупателя: вид оплаты; адреc; телефон; email.

Методы класса:
`update(Partial<customerData>): void` - частичное или полное обновление данных в модели. В качестве параметров передаются все или часть новых данных покупателя.
`get(): IBuyer` - получение всех данных покупателя.
`clear(): void` - очистка данных покупателя.
`validate(data: IBuyer): {isValid: boolean, errors: Partial<Record<keyof IBuyer, string>>}` - валидация данных, принимает в параметрах объект с данными покупателя. Возвращает булево значение - валидны они или нет. Если какие-то данные не валидны - дополнительно возвращает объект с названием невалидных полей и сообщениями об ошибке для каждого из них.

## Слой коммуникации

### Класс WebLarekApi
Класс является представителем коммуникационного слоя. Отвечает за получение данных с сервера и отправку данных на сервер. Для этого использует функциональность класса, принимаемого в конструкторе. Принимаемый в конструктор объект должен соответствовать интерфейсу IApi.

Методы класса:
`getProducts(): Promise<object>` - выполняет GET запрос на эндпоинт /product/ и возвращает объект с массивом товаров и их количеством.  
`postOrder(data: object): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт /order/. По умолчанию выполняется `POST` запрос. Возвращает объект с ответом сервера, содержащий id созданного заказа и его стоимость.

## Слой отображения

### Класс Component
Родительский класс для всех классов - элементов слоя отображения.

Методы класса:
`setImage(element: HTMLImageElement, src: string, alt?: string)` - размещение изображения в элементе-контейнере. Элемент, ссылка на изображение и альт.текст(опционально) передаются как параметры.
`render(data?: Partial<T>): HTMLElement` - наполнение элемента контентом и его возвращение.

### Класс Header
Класс отвечает за отображение "шапки" веб-страницы.

Поля класса:
`basketButtonEl: HTMLButtonElement` -  отображение кнопки корзины, при нажатии на которую открывается модельное окно - корзина.
`basketCounterEl: HTMLElement` - отображение количества товаров в корзине, при пустой корзине показан 0.

Методы класса:
`set counter(value: number)` - сеттер, который меняет значение счётчика корзины при измении количества товаров в ней.

### Класс Basket
Отображение корзины.

Поля класса:
`listEl: HTMLElement` - элемент-контенер для списка карточек товаров в корзине.
`priceEl: HTMLElement` - элемент, в котором размещена цена продукта.
`checkoutBtn: HTMLButtonElement`- кнопка для оформления заказа из корзины.

Методы класса:
`set items(items: HTMLElement[])` - сеттер для добавления массива с продуктами в корзину.
`set total(value: number)` - сеттер общей стоимости товаров в корзине.
`set checkOutEnabled(value: boolean)` - управление кнопкой оформления заказа (нет продуктов в корзине - кнопка не активна).

### Класс Gallery
Класс для отображения списка карточек с товарами.

Методы класса:
`set items(items: HTMLElement[])` - наполнение контейнера карточками, переданными в виде массива.
`clear()` - очистка галереи

### Класс Modal
Функциональность модального окна для отображения элементов типа карточки товара, корзины и т.д.

Поля класса:
`closeButton: HTMLButtonElement` - кнопка закрытия модального окна
`contentEl: HTMLElement` - элемент для размещения контента в модальном окне.

Методы класса:
`open(content: HTMLElement)` - открытие модального окна, в параметр принимает элемент для отображения.
`close()` - закрытие модального окна.

### Класс Success
Окно с информацией об успешном оформлении заказа.

Поля класса:
`descriptionEl: HTMLElement` - элемент для размещения описания.
`private buttonClose`- кнопка закрытия окна.

Методы класса:
`set total(value: number)` - сеттер общей стоимости заказа, информация о списаной сумме отображается в описании.
`set onClose` - установка функции обработчика клика на кнопку "закрыть".

### Класс Form
Общий класс для всех форм.

Поля класса:
`formEl: HTMLFormElement` - элемент form.
`confirmButton: HTMLButtonElement`- кнопка подтверждения.
`errorMessageEl: HTMLElement` - элемент для размещения ошибок в форме.

Методы класса:
`set onSubmit` - сеттер, устанавливает коллбэк-обработчиксабмита формы.
`set isValid` - сеттер. устанавливает boolean-значение валидности формы.
`abstract validate()` - абстрактный метод валидирования формы.
`abstract checkValidity()` - абстрактный метод промежуточной проверки валидности (до submit).

### Класс FormOrderStep1
Класс первой формы, которую заполняет пользователь при заказе товара.

Поля класса:
`payment: TPayment | undefined` - инфомация о выбранном типе платежа.
`paymentButtons: HTMLButtonElement[]` - кнопки, устанавливающие тип платежа.
`addressInput: HTMLInputElement` - поле для ввода адреса доставки.

Методы класса:
`set values(data: IPaymentDelivery)` - установка данных формы.
`get values(): IPaymentDelivery` - получение данных формы.
`checkValidity(): boolean` - промежуточная проверка валидности формы.
`validate(): boolean` -валидация данных, введённых в форму.
`updatePaymentUI()` - обновление UI кнопок с методами платежа, активация выбранной кнопки.

### Класс FormOrderStep2
Класс второй формы, которую заполняет пользователь при заказе товара.

Поля класса:
`emailInput: HTMLInputElement`- поле для ввода email.
`phoneInput: HTMLInputElement` - поле для ввода номера телефона.

Методы класса:
`set values(data: IPaymentDelivery)` - установка данных формы.
`get values(): IPaymentDelivery` - получение данных формы.
`checkValidity(): boolean` - промежуточная проверка валидности формы.
`validate(): boolean` -валидация данных, введённых в форму.

### Класс Card
Общий класс для всех карточек.

Методы класса:
`setText(element: HTMLElement, value: string)` - размещение текста в элементе-контейнере. Элемент и текст передаются как параметры.

### Класс CatalogCard
Класс отвечает за отображение карточки товара в каталоге.

Поля класса:
`titleEl: HTMLElement` - заголовок карточки / название товара.
`imageEl: HTMLImageElement` - изображение товара.
`categoryEl: HTMLElement` - категория, к котрой относится товар.
`priceEl: HTMLElement` - цена товара в синапсах. Если цена null отображается "Бесценно".
`_id!: string` - id товара.

Методы класса:
`set title(value: string)` - установка залоговка карточкис названием товара.
`set image(value: string)` - установка изображения товара.
`set category(value: string)` - установка категории товара.
`set price(value: number | null)` - установка цены товара.
`set id(value: string)` - установка id товара.

### Класс OpenedCard
Класс отвечает за отображение открытой карточки конкретного товара.

Поля класса:
`titleEl: HTMLElement` - заголовок карточки / название товара.
`imageEl: HTMLImageElement` - изображение товара.
`categoryEl: HTMLElement` - категория, к котрой относится товар.
`priceEl: HTMLElement` - цена товара в синапсах. Если цена null отображается "Бесценно".
`descriptionEl: HTMLElement`- описание товара.
`buttonEl: HTMLButtonElement` - кнопка "Купить"/"Недоступно"(если цена null).
`_id!: string` - id товара.

Методы класса:
`set title(value: string)` - установка залоговка карточки с названием товара.
`set image(value: string)` - установка изображения товара.
`set category(value: string)` - установка категории товара.
`set price(value: number | null)` - установка цены товара.
`set description(value: string)` - установка описания товара.
`set buttonText` - установка текста, размещённого на кнопке карточки.
`set buttonEnabled(value: boolean)` - управление доступностью кнопки submit.
`set id(value: string)` - установка id товара.

### Класс BasketCard
Класс отвечает за отображение карточки товара в корзине.

Поля класса:
`titleEl: HTMLElement` - название товара.
`priceEl: HTMLElement` - цена товара в синапсах.
`removeBtn: HTMLButtonElement`- кнопка удаления товара из корзины.
`_id!: string` - id товара.

Методы класса:
`set title(value: string)` - установка названия товара.
`set price(value: number)` - установка цены товара.
`set id(value: string)` - установка id товара.